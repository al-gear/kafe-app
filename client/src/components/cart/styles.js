import { makeStyles } from '@material-ui/core/styles';

export default makeStyles({
    table: {
        minWidth: 300,
        backgroundColor: "#cdaa7d",

    },
    root: {
        marginTop: 20,
        marginLeft: 60,
    },

});